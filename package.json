{
  "name": "rv-widget-dev-app",
  "description": "Widget developer app.",
  "version": "0.0.2",
  "main": "web/index.html",
  "scripts": {
    "dev": "build/rv-widget-dev-app/linux32/nw --remote-debugging-port=9222",
    "dev-install": "rm -rf node_modules && rm -rf web/components && npm install && bower install && gulp build"
  },
  "window": {
    "toolbar": false,
    "width": 1024,
    "height": 768
  },
  "dependencies": {
    "express": "^4.4.1",
    "request": "^2.36.0",
    "sockjs": "^0.3.9"
  },
  "devDependencies": {
    "gulp": "~3.8.0",
    "gulp-run": "^1.5.2",
    "gulp-util": "^2.2.16",
    "node-webkit-builder": "0.0.2"
  }
}
